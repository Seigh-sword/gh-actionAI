name: Ollama Issue Bot

on:
  issues:
    types: [opened]

jobs:
  ai:
    runs-on: ubuntu-latest

    steps:
      - name: Install Ollama
        run: |
          curl -fsSL https://ollama.com/install.sh | sh

      - name: Start Ollama server
        run: |
          nohup ollama serve > ollama.log 2>&1 &

          for i in {1..30}; do
            curl -s http://localhost:11434 > /dev/null && break
            echo "waiting for ollama..."
            sleep 1
          done

      - name: Pull model
        run: |
          ollama pull tinyllama

      - name: Save issue safely
        run: |
          echo "${{ github.event.issue.title }}" > title.txt
          echo "${{ github.event.issue.body }}" > body.txt

      - name: Run AI
        run: |
          TITLE=$(cat title.txt)
          BODY=$(cat body.txt)

          PROMPT="You are a helpful coding assistant. Keep answers short.

Issue Title: $TITLE
Issue Body: $BODY"

          ollama run tinyllama "$PROMPT" > response.txt

          cat response.txt

      - name: Post comment
        run: |
          RESPONSE=$(cat response.txt | sed ':a;N;$!ba;s/\n/\\n/g')

          curl -s -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -d "{\"body\":\"$RESPONSE\"}"